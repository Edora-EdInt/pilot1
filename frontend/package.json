{
  "name": "edora-v1-static-server",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency static server that serves the Edora v1 single-file app from the repo root.",
  "scripts": {
    "start": "node server.js"
  }
}
